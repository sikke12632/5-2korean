# 차시 데이터 스키마 v1

「말과 글 수첩」의 차시 데이터 표준 형식.
**JSON을 고치면 사이트가 바뀐다. HTML은 손대지 않는다.**

파일은 두 종류뿐이다.

```
lessons/<사이트 id>/units.json        ← 단원·차시 목차 (사이트당 1개)
lessons/<사이트 id>/<단원 id>/<차시코드>.json  ← 차시 내용 (차시당 1개)
```

`<사이트 id>` = `gugeo-5-2` 처럼 `<과목>-<학년>-<학기>`.
**폴더 이름은 전부 ASCII로 쓴다.** 한글 폴더명은 zip·드라이브 이동에서 깨진다.

---

## 1. `units.json` — 목차

```json
{
  "schema": "units/1",
  "site": {
    "title": "말과 글 수첩",
    "subtitle": "초등 국어 5학년 2학기",
    "nav_foot": "찾고 싶은 차시를 눌러 보세요.\n언제든 다시 와도 괜찮아요."
  },
  "units": [
    {
      "id": "u1",
      "order": 1,
      "label": "1",
      "title": "작품에 대한 의견을 나누어요",
      "lessons": [
        { "code": "04-06", "title": "똑, 똑", "guide_pages": "90~93" }
      ]
    }
  ]
}
```

| 필드 | 필수 | 설명 |
|---|---|---|
| `site.title` | ● | 사이트 이름. 왼쪽 목차 머리와 `<title>`에 쓰인다 |
| `site.subtitle` | | 목차 머리 두 번째 줄 |
| `site.nav_foot` | | 목차 맨 아래 안내문. `\n` = 줄바꿈 |
| `units[].id` | ● | **ASCII.** 폴더 이름이자 URL 조각이 된다 |
| `units[].order` | ● | 정렬 순서. **`label`로 정렬하지 않는다** |
| `units[].label` | ● | 화면에 붙는 단원 번호. 숫자면 `1. 제목`, 아니면 `제목`만 |
| `units[].title` | ● | 단원 이름 |
| `units[].lessons[].code` | ● | 차시 코드 (아래) |
| `units[].lessons[].title` | ● | 목차에 뜨는 짧은 이름 (보통 제재 이름) |
| `units[].lessons[].guide_pages` | | 집필자 참고용. **화면에 절대 나가지 않는다** |

### `order`를 따로 두는 이유

단원이 8개인데 `독서`, `매체`는 숫자가 아니다.
`label`을 정렬 키로 쓰면 문자열 정렬이 되어 순서가 깨진다.
**정렬은 언제나 `order`, 표시는 언제나 `label`.**

### 차시 코드 규칙

| 코드 | 뜻 | 화면 표기 |
|---|---|---|
| `01` | 1차시 | `1차시` |
| `04-06` | 4~6차시 | `4~6차시` |
| `09-11` | 9~11차시 | `9~11차시` |

- 두 자리로 0을 채운다. 파일 이름 정렬과 코드 정렬이 어긋나지 않게.
- 하이픈은 **범위**다. 단일 차시도 범위형 파서를 통과한다.
- 정렬 키는 첫 숫자다. 문자열 정렬이 아니다.

---

## 2. `<차시코드>.json` — 차시 내용

```json
{
  "schema": "lesson/1",
  "unit": "u1",
  "code": "04-06",
  "title": "「똑, 똑」을 읽고\n의견을 나누어요",
  "goal": "시에 대한 의견을 나눌 수 있어요.",
  "textbook_pages": "42~48",
  "guide_pages": "90~93",
  "items": [ ... ],
  "extra_task": null
}
```

| 필드 | 필수 | 설명 |
|---|---|---|
| `schema` | ● | 항상 `"lesson/1"` |
| `unit` | ● | `units.json`의 단원 `id`와 일치해야 한다 |
| `code` | ● | 파일 이름과 일치해야 한다 |
| `title` | ● | 본문 큰 제목. `\n` = 줄바꿈 |
| `goal` | | 학습 목표 한 줄. 초록 세로선 |
| `textbook_pages` | ● | **교과서 쪽.** 화면에 나가는 유일한 쪽수 |
| `guide_pages` | | **지도서 쪽. HTML 주석으로만 나간다** |
| `items` | ● | 항목 배열. 순서 = 화면 번호 |
| `extra_task` | | 과제 3칸 아래 한 칸 더. 없으면 `null` |

### 쪽수 규칙

**화면에 나가는 쪽수는 교과서뿐이다.**
`guide_pages`는 빌드 시 `<!-- 근거: 지도서 90~93쪽 -->` 주석으로만 들어간다.
아이는 지도서를 볼 수 없다. 교사 대조용 근거는 소스에 있으면 충분하다.

빌드 스크립트가 이걸 검사한다 — `python build_gugeo.py --check`.

### `items[]` — 항목

```json
{
  "title": "'똑, 똑'은 어떤 소리일까",
  "pages": "44",
  "guide_page": "91",
  "open": true,
  "blocks": [ ... ]
}
```

| 필드 | 필수 | 설명 |
|---|---|---|
| `title` | ● | 접힘 상태에서 보이는 줄. 이것만 읽고 무엇인지 알아야 한다 |
| `pages` | | 교과서 쪽. `"44"` 또는 `"46~48"` |
| `guide_page` | | 지도서 쪽. 주석 전용 |
| `open` | | `true`면 처음부터 펼침. 보통 첫 항목만 |
| `blocks` | ● | 본문 블록 배열 |

**번호는 데이터에 넣지 않는다.** 배열 순서에서 자동으로 나온다.
항목을 끼워 넣으면 번호가 알아서 밀린다.

**항목은 자기완결이어야 한다.** 다른 항목을 가리키는 문장을 쓰지 않는다.
"위 3번의 두 가지가 다 들어가게" 같은 문장은 3번을 안 본 아이에게 통하지 않는다.

---

## 3. 블록 타입 (6개)

블록 타입은 **닫힌 집합**이다. 새 타입은 여러 차시에서 반복될 때만 추가한다.
인라인 강조는 `**굵게**` 하나만 된다. 그 외 HTML은 전부 이스케이프된다.

### `text` — 문단

```json
{ "type": "text", "text": "같은 **'똑, 똑'**인데 연마다 가리키는 것이 달라요." }
```

### `list` — 목록

```json
{ "type": "list", "items": ["1연 — 눈물이 떨어지는 소리", "2연 — 샤프심이 부러지는 소리"] }
```

### `word` — 낱말 뜻

```json
{
  "type": "word",
  "term": "애꿎다",
  "senses": ["아무런 잘못 없이 억울하다.", "그 일과는 아무런 상관이 없다."],
  "used": 2,
  "note": "샤프심은 아무 잘못이 없는데 괜히 부러뜨린 거예요."
}
```

`used`는 1부터 센다. `2`면 화면에 `②의 뜻으로 쓰였어요`가 붙는다. 없으면 그 줄이 빠진다.

### `shape` — 답의 모양

```json
{ "type": "shape", "parts": ["시에 있는 표현", "내 경험이나 생각"] }
```

칸 사이에 `＋`가 자동으로 들어간다. **답이 아니라 답의 구성 요소**를 보여주는 자리다.
오픈북 대응의 핵심 블록이다 — 통째로 베낄 것이 없다.

### `example` — 예시 답안 카드

```json
{
  "type": "example",
  "tag": "이렇게 쓰면 좋아요",
  "quote": "몰라주니까 눈물이 난다",
  "why": "나도 누가 내 마음을 몰라줄 때 눈물이 났던 적이 있어서 공감했어요."
}
```

`why`는 없어도 된다 (질문 예시처럼 인용문만 필요한 경우).

**한 항목에 예시를 두 개 이상 넣는다.** 하나만 두면 정답이 하나라는 인상을 준다.
`tag`를 `이렇게 쓰면 좋아요` / `이렇게도 좋아요`로 다르게 붙여 둘 다 정답임을 보인다.

**지도서 예시 답안을 그대로 옮기지 않는다.** 같은 학습 문제에 대한 답을 직접 쓴다.
인용 부분(`quote`)은 작품 원문이라 그대로 두되, `why`는 우리 문장으로 쓴다.

### `note` — 꼬리말

```json
{ "type": "note", "text": "교과서 44쪽 (1)번·(2)번 문제와 이어져요." }
```

점선 위 작은 회색 글씨. 보통 항목 맨 끝.

---

## 4. 데이터에 넣지 않는 것

| 넣지 않는 것 | 어디에 있나 |
|---|---|
| 과제 3칸 문구 | 템플릿 고정. 차시마다 바뀌면 루틴이 안 된다 |
| 질문 만들기 보기 3개 | 템플릿 고정 |
| 항목 번호 | 배열 순서에서 계산 |
| 진도 점 개수 | 항목 개수에서 계산 |
| CSS·HTML | `build_gugeo.py`가 생성 |

그리고 **지도서 원문에 없는 내용은 만들지 않는다.**
채울 것이 없으면 그 항목을 비운다. 외부 자료를 끌어오지 않는다.
교과서에 이미 있는 활동을 과제로 반복하지 않는다.

---

## 5. 빌드

```
python build_gugeo.py            # lessons/ → site/
python build_gugeo.py --check    # 화면 텍스트에 '지도서'가 남았는지 검사
```

빌드 결과:

```
site/gugeo-5-2/index.html      ← 첫 차시로 보내는 입구
site/gugeo-5-2/u1/04-06.html   ← 차시 페이지
```

**페이지 하나가 파일 하나로 완결된다.** CSS·JS를 안에 넣는다.
외부 파일로 빼면 드라이브 미리보기, 메일 첨부, USB에서 서식이 깨진다.
어차피 빌드가 전체를 다시 찍으므로 고칠 곳은 `build_gugeo.py`의 `STYLE` 하나다.

JSON이 없는 차시는 목차에 흐린 글씨로 남고 링크가 걸리지 않는다.
차시를 하나 추가하려면 JSON 하나를 놓고 다시 빌드하면 된다.

빌드는 아래를 검사하고, 하나라도 걸리면 **멈춘다**.

- `schema`·`unit`·`code`가 파일 위치와 맞는지
- `units.json` 차시 목록에 있는 차시인지
- 항목에 `title`과 `blocks`가 있는지
- 블록 타입이 6개 중 하나인지, 필수 필드가 있는지
- 화면 텍스트에 `지도서`가 남았는지
